# QA scoring model

The rubric is deterministic and small on purpose. It rewards basic call discipline: greeting, verification, issue capture, safe handling of sensitive data, correct escalation behavior, a resolution code, a usable summary, and attention to sentiment risk.

The score is not a benchmark or a benchmark proxy. It is a call-review heuristic meant to keep the local demo honest and reviewable.
