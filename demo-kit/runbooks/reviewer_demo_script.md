# Reviewer demo script

Use the insurance intake scenario to verify that PII redaction stays visible in the UI without exposing the original value. Use the appointment reschedule scenario to verify a clean resolution path.
